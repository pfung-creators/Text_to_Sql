# Text_to_Sql
AI-powered text-to-SQL interface that translates natural language into safe, validated SQL queries against a PostgreSQL database with guardrails against destructive operations, LLM-based hallucination detection via back-translation and confidence scoring, and vector-search-powered dynamic few-shot retrieval (RAG) that improves from user feedback.
